---

- import_tasks: pre_checks.yml

- include_tasks: "check_configuration_{{ ansible_os_family | lower }}.yml"
  when: check_mode == "configuration"
  vars:
    hana_vip: "10.1.2.6"

- import_tasks: post_checks.yml
