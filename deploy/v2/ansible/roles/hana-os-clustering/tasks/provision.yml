---

# Pre-provisioning tasks
- name: Ensure clustering software is installed
  package:
    name: "{{ item }}"
    state: present
  with_items: "{{ ha_packages[ansible_os_family] }}"

- name: Ensure Clustering Service is started and enabled
  systemd:
    name: "{{ clustering_service[ansible_os_family] }}"
    enabled: true
    state: started

# Pacemaker can create a large number of processes
- name: Ensure Process limit is raised
  lineinfile:
    path: /etc/systemd/system.conf
    state: present
    regexp: "^#?\s*DefaultTasksMax="
    line: "DefaultTasksMax=4096"
  register: raise_process_limit

- name: Ensure systemctl daemon is reloaded
  systemd:
    daemon_reload: True

- name: Ensure clustering can manage Virtual IPs
  lineinfile:
    path: /etc/sysconfig/network/ifcfg-eth0   # @TODO is this the right adapter? Do we want the DB adapter?
    state: present
    regexp: "^#?\s*CLOUD_NETCONFIG_MANAGE="
    line: "CLOUD_NETCONFIG_MANAGE='no'"


# @TODO configure SSH access between node (swap SSH public keys)?

#########
# Load balancer details? The IP should be retrieved from the output.json details
#
# @TODO Add Load Balancer front end IP to output JSON file.

# SLES Clustering
# Ref: https://documentation.suse.com/sle-ha/12-SP4/html/SLE-HA-install-quick/index.html

# @TODO NTP server for datetime sync?

# Set up Hosts entries for the cluster nodes
- name: Ensure the Primary node hosts entry exits
  lineinfile:
    path: /etc/hosts
    state: present
    insertafter: EOF
    regexp: "{{ hana_database.nodes[0].dbname }}"
    line: "{{ hana_database.nodes[0].ip_db_nic }} {{ hana_database.nodes[0].dbname }}"

- name: Ensure the Secondary node hosts entry exits
  lineinfile:
    path: /etc/hosts
    state: present
    insertafter: EOF
    regexp: "{{ hana_database.nodes[1].dbname }}"
    line: "{{ hana_database.nodes[1].ip_db_nic }} {{ hana_database.nodes[1].dbname }}"

# https://rpm.pbone.net/index.php3/stat/45/idpl/27916721/numer/8/nazwa/ha-cluster-init
- name: Ensure Primary node initiates the Cluster
  when: ansible_hostname == hana_database.nodes[0].dbname
  block:
#   1. SSH keys for passwordless - @TODO?
#   2. csync2 config
    - name: Ensure csync2 is configured
      shell: crm cluster init -y csync2

#   3. corosync config
    - name: Ensure corosync is configured
      shell: crm cluster init -y -u corosync

#   4. Start Cluster # @TODO is this the right interface?
    - name: Ensure cluster (hdb_{{ sid | upper }}) is configured
      shell: "crm cluster init -y cluster --name 'hdb_{{ sid | upper }}' --interface eth0"

- name: Ensure Secondary node joins the Cluster
  when: ansible_hostname == hana_database.nodes[1].dbname
  block:
    - name: Ensure the configuration files are synchronised
      shell: "crm cluster join -y -c {{ hana_database.nodes[0].ip_db_nic }} csync2"

    - name: Ensure the cluster is joined
      shell: "crm cluster join -y -c {{ hana_database.nodes[0].ip_db_nic }} cluster"

- name: Ensure HA Cluster password is set to something secure
  user:
    name: hacluster
    password: "{{ ha_cluster_password|password_hash('sha512') }}" # @TODO set variable

- name: Ensure cluster configuration contains correct details
  template:
    src: corosync.conf.j2
    dest: /etc/corosync/corosync.conf
    mode: 0600

- name: Ensure the Corosync service is restarted
  systemd:
    name: "{{ clustering_service[ansible_os_family] }}"
    state: restarted

# @TODO - configure stonith - primary node
- name: Ensure the Cluester STONITH is configured
  when: ansible_hostname == hana_database.nodes[0].dbname
  block:
    # 1. Enable maintenance mode
    - name: Ensure maintenance mode is enabled
      shell: "crm configure property maintenance-mode=true"

    # 2. Set defaults
    #    - property "cib-bootstrap-options"
    - name: Ensure CIB Bootstrap Options are set
      shell: >
        crm configure property \$id="cib-bootstrap-options"
        no-quorum-policy="ignore"
        stonith-enabled="true"
        stonith-action="reboot"
        stonith-timeout="150s"

    #    - rsc_defaults "rsc-options"
    - name: Ensure Default Resource configuration is set
      shell: >
        crm configure rsc_defaults \$id="rsc-defaults"
        resource-stickiness="1000"
        migration-threshold="5000"
        op_defaults \$id="op-options"
        timeout="600"

    # 3. Create service Principal

    # 4. Assign service principal as owner of DB VMs

    # 5. Configure STONITH fencing devices
    #    - primitive rsc_st_azure_1 stonith:fence_azure_arm
    - name: Ensure the Primary STONITH fencing device is created
      shell: >
        crm configure primitive rsc_st_azure_1 stonith:fence_azure_arm params
        subscriptionId="{{ @TODO - SUBSCRIPTION_ID }}"
        resourceGroup="{{ infrastructure.resource_group.name }}"
        tenantId="{{ @TODO - AZURE_ACTIVE_DIRECTORY_ID }}"
        login="{{ @TODO - SERVICE_PRINCIPAL_APPLICATION_ID }}"
        passwd="{{ @TODO - SERVICE_PRINCIPAL_APPLICATION_PASSWORD }}"
        action="off"

    #    - primitive rsc_st_azure_2 stonith:fence_azure_arm
    - name: Ensure the Secondary STONITH fencing device is created
      shell: >
        crm configure primitive rsc_st_azure_2 stonith:fence_azure_arm params
        subscriptionId="{{ @TODO - SUBSCRIPTION_ID }}"
        resourceGroup="{{ @TODO - RESOURCE_GROUP_NAME }}"
        tenantId="{{ @TODO - AZURE_ACTIVE_DIRECTORY_ID }}"
        login="{{ @TODO - SERVICE_PRINCIPAL_APPLICATION_ID }}"
        passwd="{{ @TODO - SERVICE_PRINCIPAL_APPLICATION_PASSWORD }}"
        action="off"

    #    - colocation col_st_azure -2000: rsc_st_azure_1:Started rsc_st_azure_2:Started
    - name: Ensure STONITH fencing device colocation is configured
      shell: >
        crm configure colocation col_st_azure -2000: rsc_st_azure_1:Started rsc_st_azure_2:Started

    # 6. Define Topology
    #    - SAPHanaTopology resource
    - name: Ensure SAP HANA Topology resource is configured
      shell: >
        crm configure primitive rsc_SAPHanaTopology_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
        ocf:suse:SAPHanaTopology
        operations \$id="rsc_sap2_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}-operations"
        op monitor interval="10" timeout="600"
        op start interval="0" timeout="600"
        op stop interval="0" timeout="300"
        params SID="{{ hana_database.instance.sid | upper }}" InstanceNumber="{{ hana_database.instance.instance_number }}"

    #    - SAPHanaTopology clone
    - name: Ensure SAP HANA Topology resource is configured
      shell: >
        crm configure clone cln_SAPHanaTopology_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
        rsc_SAPHanaTopology_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
        meta is-managed="true" clone-node-max="1" target-role="Started" interleave="true"

    # 7. Configure the HANA resources
    #    - primitive SAPHana resource wit operations
    - name: Ensure SAP HANA Topology resource is configured
      shell: >
        crm configure primitive rsc_SAPHana_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
        ocf:suse:SAPHana
        operations \$id="rsc_sap_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}-operations"
        op start interval="0" timeout="3600"
        op stop interval="0" timeout="3600"
        op promote interval="0" timeout="3600"
        op monitor interval="60" role="Master" timeout="700"
        op monitor interval="61" role="Slave" timeout="700"
        params
        SID="{{ hana_database.instance.sid | upper }}"
        InstanceNumber="{{ hana_database.instance.instance_number }}"
        PREFER_SITE_TAKEOVER="true"
        DUPLICATE_PRIMARY_TIMEOUT="7200"
        AUTOMATED_REGISTER="true"

    #    - master slave
    - name: Ensure SAP HANA Topology resource is configured
      shell: >
        crm configure ms msl_SAPHana_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
          rsc_SAPHana_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
          meta is-managed="true" notify="true" clone-max="2" clone-node-max="1"
          target-role="Started" interleave="true"

    #    - primitive heartbeat/IP Address
    - name: Ensure SAP HANA Topology resource is configured
      shell: >
        crm configure primitive rsc_ip_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }} ocf:heartbeat:IPaddr2
          meta target-role="Started" is-managed="true"
          operations \$id="rsc_ip_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}-operations"
          op monitor interval="10s" timeout="20s"
          params ip="{{ hana_database.loadbalancer.frontend_ip }}"

    #    - primitive anything (?)
    - name: Ensure SAP HANA Topology resource is configured
      shell: >
        crm configure primitive rsc_nc_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }} anything
          params binfile="/usr/bin/nc" cmdline_options="-l -k 62500"
          op monitor timeout=20s interval=10 depth=0

    #    - group
    - name: Ensure SAP HANA Topology resource is configured
      shell: >
        crm configure group g_ip_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
          rsc_ip_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
          rsc_nc_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}

    #    - colocation IP resource
    - name: Ensure SAP HANA Topology resource is configured
      shell: >
        crm configure colocation col_saphana_ip_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
          2000:
          g_ip_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}:Started
          msl_SAPHana_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}:Master

    #    - colocation Order resource
    - name: Ensure SAP HANA Topology resource is configured
      shell: >
        crm configure order ord_SAPHana_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
          2000:
          cln_SAPHanaTopology_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}
          msl_SAPHana_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}

    # 8. Resource clean up
    - name: Ensure cluster resources are in a healthy state
      shell: "crm resource cleanup rsc_SAPHana_{{ hana_database.instance.sid | upper }}_HDB{{ hana_database.instance.instance_number }}"

    # 9. Disable maintenance mode
    - name: Ensure maintenance mode is disabled
      shell: "crm configure property maintenance-mode=false"









#####################
# pcs commands below for configuring the stonith parameters


# - name: OS Cluster Tasks should run on the "Master" node only
#   when: is_hana_primary_node
#   block:
#     - name: "Start: Provision OS cluster optimization"
#       debug:
#         msg: "Provision OS cluster optimization: Starting..."


#     - name: Check STONITH method is defined
#       ignore_errors: yes
#       shell: >
#         {{ pacemaker_cli_command }} stonith show | grep {{ pacemaker_cluster_stonith_resource_names_base }}
#       args:
#         executable: /bin/bash
#       register: stonith_status
#       become: yes
#       failed_when: stonith_status.rc == 2

#     - name: "Ensure STONITH method is defined and working for all nodes"
#       become: yes
#       command: "{{ pacemaker_cli_command }} stonith create {{ item }} {{ pacemaker_cluster_stonith_fence_name }} subscriptionId={{ azure_subscription_id }} resourceGroup={{ azure_resource_group_name }} tenantId={{ azure_active_directory_tenant_id }} login={{ pacemaker_cluster_stonith_app_username }} passwd={{ pacemaker_cluster_stonith_app_password }} pcmk_off_timeout={{ pacemaker_cluster_off_timeout }} pcmk_status_timeout={{ pacemaker_cluster_status_timeout }} power_timeout={{ pacemaker_cluster_power_timeout }} pcmk_reboot_timeout={{ pacemaker_cluster_reboot_timeout }} pcmk_reboot_retries={{ pacemaker_cluster_reboot_retries }} pcmk_off_retries={{ pacemaker_cluster_poweroff_retries }} pcmk_list_timeout={{ pacemaker_cluster_list_timeout }} pcmk_list_retries={{ pacemaker_cluster_list_retries }} pcmk_monitor_timeout={{ pacemaker_cluster_monitor_timeout }} pcmk_monitor_retries={{ pacemaker_cluster_monitor_retries }} pcmk_status_retries={{ pacemaker_cluster_status_retries }} network-fencing={{ pacemaker_cluster_network_fencing_status }} pcmk_host_map={{ hostname_prefix }}0:{{ vm_prefix }}0;{{ hostname_prefix }}1:{{ vm_prefix }}1"
#       with_items: "{{ pacemaker_cluster_stonith_resource_names }}"
#       when: stonith_status.rc == 1

#     - name: "Ensure STONITH is enabled"
#       become: yes
#       command: "{{ pacemaker_cli_command }} property set stonith-enabled=true"


#     - name: Check resource {{ pacemaker_cluster_load_balancer_ip_resource_name }}
#       become: yes
#       ignore_errors: yes
#       command: "{{ pacemaker_cli_command }} resource show {{ pacemaker_cluster_load_balancer_ip_resource_name }}"
#       register: pclbirn_resource

#     - name: "Ensure the IP address (from Load Balancer) exists in the resource group"
#       become: yes
#       command: "{{ pacemaker_cli_command }} resource create {{ pacemaker_cluster_load_balancer_ip_resource_name }} IPaddr2 ip={{ pacemaker_cluster_load_balancer_ip_address }} --group {{ pacemaker_cluster_resource_group }}"
#       when: pclbirn_resource.rc != 0


#     - name: Check resource {{ pacemaker_cluster_load_balancer_network_card_resource_name }}
#       become: yes
#       ignore_errors: yes
#       command: "{{ pacemaker_cli_command }} resource show {{ pacemaker_cluster_load_balancer_network_card_resource_name }}"
#       register: pclbncrn_resource

#     - name: "Ensure the Azure Load Balancer health-check exists in the resource group"
#       become: yes
#       command: "{{ pacemaker_cli_command }} resource create {{ pacemaker_cluster_load_balancer_network_card_resource_name }} azure-lb port={{ pacemaker_cluster_load_balancer_network_card_port }} --group {{ pacemaker_cluster_resource_group }}"
#       when: pclbirn_resource.rc != 0



#     - name: Check resource {{ pacemaker_cluster_hana_topology_resource_name }}
#       become: yes
#       ignore_errors: yes
#       command: "{{ pacemaker_cli_command }} resource show {{ pacemaker_cluster_hana_topology_resource_name }}"
#       register: pchtrn_resource

#     - name: "Ensure the HANA Topology resource is correctly configured"
#       become: yes
#       command: "{{ pacemaker_cli_command }} resource create {{ pacemaker_cluster_hana_topology_resource_name }} SAPHanaTopology SID={{ sid }} InstanceNumber={{ pacemaker_cluster_hana_instance_number }} --clone clone-max={{ pacemaker_cluster_hana_clone_max }} clone-node-max={{ pacemaker_cluster_hana_clone_node_max }} interleave={{ pacemaker_cluster_hana_interleave }}"
#       when: pchtrn_resource.rc != 0


#     - name: Check resource {{ pacemaker_cluster_hana_resource_name }}
#       become: yes
#       ignore_errors: yes
#       command: "{{ pacemaker_cli_command }} resource show {{ pacemaker_cluster_hana_resource_name }}"
#       register: pchrn_resource

#     - name: "Ensure the HANA Master/Slave set resource exists"
#       become: yes
#       command: "{{ pacemaker_cli_command }} resource create {{ pacemaker_cluster_hana_resource_name }} SAPHana SID={{ sid }} InstanceNumber={{ pacemaker_cluster_hana_instance_number }} PREFER_SITE_TAKEOVER=true DUPLICATE_PRIMARY_TIMEOUT=7200 AUTOMATED_REGISTER=true master notify=true clone-max={{ pacemaker_cluster_hana_clone_max }} clone-node-max={{ pacemaker_cluster_hana_clone_node_max }} interleave={{ pacemaker_cluster_hana_interleave }} timeout={{ pacemaker_cluster_hana_master_slave_set_timeout }}"
#       when: pchrn_resource.rc != 0

#     - name: Check order constraint
#       become: yes
#       ignore_errors: yes
#       command: "{{ pacemaker_cli_command }} constraint order show"
#       register: order_constraint

#     - name: "Ensure only one concurrent master is allowed"
#       become: yes
#       command: "{{ pacemaker_cli_command }} constraint order {{ pacemaker_cluster_hana_topology_resource_name }}-clone then {{ pacemaker_cluster_hana_resource_name }}-master symmetrical=false"
#       when: order_constraint.stdout.split(":")[1] | trim == ""


#     - name: Check colocation constraint for {{ pacemaker_cluster_resource_group }}
#       become: yes
#       ignore_errors: yes
#       shell: >
#         {{ pacemaker_cli_command }} constraint colocation show | grep {{ pacemaker_cluster_resource_group }}
#       args:
#         executable: /bin/bash
#       register: pcrg_constraint
#       failed_when: pcrg_constraint.rc == 2

#     - name: "Ensure the IP resource group follows the master"
#       become: yes
#       command: "{{ pacemaker_cli_command }} constraint colocation add {{ pacemaker_cluster_resource_group }} with master {{ pacemaker_cluster_hana_resource_name }}-master 2000"
#       when: pcrg_constraint.rc == 1

#     - name: Ensure cluster is stable
#       become: yes
#       shell: |
#         source ~/.bashrc
#         {{ pacemaker_cli_command }} status --full | grep hana_{{ sid | lower }}_sync_state
#       args:
#         executable: /bin/bash
#       register: cluster_status
#       changed_when: false
#       retries: 90
#       delay: 30
#       until: '("PRIM" in cluster_status.stdout) and ("SOK" in cluster_status.stdout)'

#     - name: "End: Provision OS cluster optimization"
#       debug:
#         msg: "Provision OS cluster optimization: ...ending"
